services:
  asistente-financiero:
    build: .
    container_name: reto_ia_container
    ports:
      - "8501:8501"
      - "3001:3001"
    volumes:
      - .:/app
    env_file:
      - .env
    environment:
      - WATCHDOG_USE_POLLING=true